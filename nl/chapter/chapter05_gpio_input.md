# Werken met Input (GPIO)

In dit hoofdstuk:

* leren we te werken met input via GPIO's
* kijken we verder naar loops via de "do while"-constructie
* bekijken we het switch-statements (veel gebruikt bij statemachine's)
